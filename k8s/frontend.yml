apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: ecommerce-devops
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: dheerajkadve/frontend:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: ecommerce-devops
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: frontend
